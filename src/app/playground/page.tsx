import BasePage from "@/components/Playground/BasePage";

const Playground = () => {
  return (
    <BasePage
      pageTitle="Welcome to Playground"
      pageDescription="This is a dedicated space for testing and experimenting the components that we've built, ensuring they function correctly before being integrated into the app"
      withPreview={false}
    >
      <></>
    </BasePage>
  )
}

export default Playground;
